Привет! Я новичок в машинном обучении, но у меня есть базовые знания Python и огромное желание создать свой собственный ИИ-агент для комплексного анализа безопасности проектов. Агент должен анализировать как серверную часть (конфигурации, логи, сетевые сервисы, бэкенд-код), так и клиентскую/пользовательскую сторону (фронтенд, cookies, сессии, пользовательский ввод, XSS/CSRF).

Я хочу, чтобы этот агент был обучен мной, а не просто использовал внешние API. То есть мне нужны собственные модели, которые я обучу на сгенерированных или собранных данных.

Что должен уметь агент в итоге:

· Принимать на вход различные артефакты: IP/домен (для запуска nmap, nikto и др.), файлы конфигураций (nginx, Apache, SSH, Docker), логи (auth.log, access.log), исходный код (Python, JavaScript, PHP), данные о пользовательских сессиях (cookies, заголовки).
· Проводить анализ и выдавать структурированный отчёт: обнаруженные уязвимости, оценка риска, рекомендации по исправлению, ссылки на стандарты (CIS, OWASP, NIST).
· Работать как единый инструмент с интерфейсом (Streamlit или CLI), который я могу запускать на своём компьютере или сервере.

Мои ресурсы и ограничения:

· Базовый Python, нет глубоких знаний ML, но готов учиться.
· Доступ к Google Colab (бесплатная версия) и, возможно, локальная видеокарта с 8 ГБ VRAM.
· Могу использовать LLM для генерации синтетических данных, но хочу сам обучать модели.
· Время: несколько недель, но хочу видеть прогресс уже в первые дни.

Твоя роль:
Ты — опытный наставник в области ML и кибербезопасности. Ты должен провести меня через все этапы создания агента.

Формат работы:

1. Сначала задай мне уточняющие вопросы, чтобы понять приоритеты: какие подзадачи анализа важнее всего (например, анализ конфигов, логов, кода), какие данные у меня уже есть, какой формат отчёта нужен.
2. Затем предложи общую архитектуру агента (модульную или монолитную), объясни плюсы и минусы.
3. Разработай детальный пошаговый план создания MVP (минимально жизнеспособного продукта), который я смогу выполнить за 2-3 недели. План должен включать:
   · Определение подзадач и моделей для каждой (например, классификация логов, анализ конфигов, детекция XSS в коде).
   · Сбор и генерацию синтетических данных с помощью LLM (ты поможешь составить промты для генерации и напишешь Python-скрипты для вызова LLM через API и сохранения в CSV).
   · Разметку и предобработку данных.
   · Выбор архитектур моделей (fine-tuning BERT, LSTM, CNN и т.д.) и обучение с кодом на PyTorch/Transformers.
   · Оценку качества моделей и сохранение.
   · Создание интерфейса (Streamlit или FastAPI + CLI), который объединяет все модели и инструменты.
4. Для каждого шага предоставляй готовый код с комментариями, который я могу запустить в Colab или локально. Объясняй ключевые концепции простыми словами.
5. Помоги мне настроить процесс генерации данных: ты предложишь промты для LLM, которые создадут примеры конфигов, логов, кода с метками, а затем я обучу на них свои модели.
6. После создания MVP дай рекомендации по улучшению: добавление RAG для базы знаний, интеграция с внешними сканерами, использование LLM для генерации текстовых описаний на основе результатов моих моделей.

Важно: подчеркивай, что финальные модели — мои собственные, обученные на данных, которые я сгенерировал и разметил. LLM используется только как генератор данных и, возможно, как помощник в написании кода, но не как замена обученных моделей.

Начни с уточняющих вопросов и предложения высокоуровневого плана. Затем шаг за шагом веди меня, давая код и объяснения. Если на каком-то этапе нужны дополнительные данные или уточнения, спрашивай.